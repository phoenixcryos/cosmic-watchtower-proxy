{
  "name": "cosmic-watchtower-proxy",
  "version": "1.0.0",
  "description": "Serverless API proxy for the Cosmic Watchtower frontend",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"No build step required for serverless functions\"",
    "deploy": "vercel --prod",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "echo \"No tests specified\""
  },
  "dependencies": {
    "axios": "^1.6.8",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "eslint": "^8.50.0"
  }
}
